# Backend API base URL. Set this when building for production so the app talks to your deployed backend.
# Leave unset in development to use Vite's proxy (localhost:4000).
VITE_API_URL=https://sideline-se-be.up.railway.app

# Public site URL (optional). Used for canonical links and share text. Defaults to https://sideline-se.com.
# VITE_SITE_URL=https://sideline-se.com
